name: infra tf  Automation

on:
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'warning'
        type: choice
        options:
          - info
          - warning
          - debug
jobs:
  automate_infra_deployment :
    runs-on : ubuntu-latest
    env:
      gcp_credential: ${{secrets.GCP_CRED}}
      SERVICE_ACCOUNT: 'naveen-master-sa@inviz-gcp.iam.gserviceaccount.com'
    steps :
      - uses: actions/checkout@v2
      - name: test
        run: |
          echo $gcp_credential > temp.json
          sudo snap install google-cloud-cli --classic
          gcloud auth activate-service-account $SERVICE_ACCOUNT --key-file=temp.json
          sudo snap install terraform --classic 
          terraform init 
          terraform plan -out=planfile
          terraform apply planfile
          rm temp.json
